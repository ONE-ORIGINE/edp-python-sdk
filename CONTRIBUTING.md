# Contributing

## Workflow
1. Fork the repository.
2. Create a feature branch.
3. Run `pytest`.
4. Run `edp-release-check`.
5. Open a pull request with a clear technical summary.

## Standards
- keep the canonical model backward compatible unless versioned explicitly
- add tests for runtime, protocol, or language changes
- update docs for new CLI or protocol commands
